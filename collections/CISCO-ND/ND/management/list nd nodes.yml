info:
  name: list nd nodes
  type: http
  seq: 1

http:
  method: GET
  url: https://{{server-address}}/api/config/class/nodes
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  body:
    type: json
    data: |-
      {
      "userName": "admin",
      "userPasswd": "!@34Qwer",
      "domain": "local"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
