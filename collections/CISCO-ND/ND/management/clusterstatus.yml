info:
  name: clusterstatus
  type: http
  seq: 2

http:
  method: GET
  url: https://{{server-address}}/clusterstatus/install
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  body:
    type: json
    data: |-
      {
      "userName": "admin",
      "userPasswd": "!@34Qwer",
      "domain": "local"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
