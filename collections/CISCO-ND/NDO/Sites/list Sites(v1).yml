info:
  name: list Sites(v1)
  type: http
  seq: 2

http:
  method: GET
  url: https://{{server-address}}/api/v1/sites
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
