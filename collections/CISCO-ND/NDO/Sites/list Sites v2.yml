info:
  name: list Sites v2
  type: http
  seq: 3

http:
  method: GET
  url: https://{{server-address}}/api/config/class/v2/sites/
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
