info:
  name: list Sites (v3)
  type: http
  seq: 5

http:
  method: GET
  url: https://{{server-address}}/api/sitemgr/v3/sites/
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
