info:
  name: list tenant
  type: http
  seq: 1

http:
  method: GET
  url: https://{{server-address}}/api/v1/tenants
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
