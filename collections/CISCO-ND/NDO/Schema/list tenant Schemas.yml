info:
  name: list tenant Schemas
  type: http
  seq: 10

http:
  method: GET
  url: https://{{server-address}}/api/v1/tenants/schemas
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
