info:
  name: list templates
  type: http
  seq: 2

http:
  method: GET
  url: https://{{server-address}}/api/v1/status/schemas/:schema-id
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: schema-id
      value: 68d4959f925970dae889ecb5
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
