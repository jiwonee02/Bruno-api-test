info:
  name: deploy template
  type: http
  seq: 11

http:
  method: GET
  url: https://{{server-address}}/api/v1/execute/schema/:schema-id/template/:template-name
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: schema-id
      value: 686cb7c91f0000491c8bb157
      type: path
    - name: template-name
      value: Test_Ocean
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
