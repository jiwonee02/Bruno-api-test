info:
  name: list template
  type: http
  seq: 3

http:
  method: GET
  url: https://{{server-address}}/api/v1/templates
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
