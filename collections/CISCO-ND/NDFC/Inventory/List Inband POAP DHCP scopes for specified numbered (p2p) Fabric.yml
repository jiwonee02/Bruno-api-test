info:
  name: List Inband POAP DHCP scopes for specified numbered (p2p) Fabric
  type: http
  seq: 2

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/lan-fabric/rest/control/fabrics/:fabricName/inventory/poap/dhcpscopes
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: fabricName
      value: FC_sw
      type: path
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
