info:
  name: List Interface groups for the given Fabric with type
  type: http
  seq: 10

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/lan-fabric/rest/control/fabrics/:fabric-name/ifgroupwithType
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: fabric-name
      value: FC_sw
      type: path
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
