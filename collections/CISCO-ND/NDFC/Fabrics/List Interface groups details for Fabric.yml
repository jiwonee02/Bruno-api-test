info:
  name: List Interface groups details for Fabric
  type: http
  seq: 9

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/lan-fabric/rest/control/fabrics/:fabric-name/ifgroups/details
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: fabric-name
      value: FC_sw
      type: path
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
