info:
  name: Get Alarm Count By Category2
  type: http
  seq: 11

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/preport/report/dashboard/main/count?count=10
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: count
      value: "10"
      type: query
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
