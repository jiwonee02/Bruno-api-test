info:
  name: Get Alarms List with filter
  type: http
  seq: 3

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/alarm/alarms/alarmlist?reqParam
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: reqParam
      value: ""
      type: query
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
