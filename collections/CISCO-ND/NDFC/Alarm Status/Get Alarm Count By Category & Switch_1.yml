info:
  name: Get Alarm Count By Category & Switch_1
  type: http
  seq: 15

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/alarm/alarms/raisedalarmswitchcategorycount?device=
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: device
      value: ""
      type: query
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
