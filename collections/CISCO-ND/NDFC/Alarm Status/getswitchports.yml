info:
  name: getswitchports
  type: http
  seq: 8

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/alarm/alarms/getswitchports?ipAddress=10.100.199.71&physical=true
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: ipAddress
      value: 10.100.199.71
      type: query
    - name: physical
      value: "true"
      type: query
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
