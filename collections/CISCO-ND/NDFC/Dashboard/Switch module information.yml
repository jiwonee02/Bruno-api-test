info:
  name: Switch module information
  type: http
  seq: 6

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/lan-fabric/rest/dashboard/switch/module?switchId=14350
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: switchId
      value: "14350"
      type: query
      description: switch db id
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
