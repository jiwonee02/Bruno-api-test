info:
  name: Switch overview
  type: http
  seq: 4

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/lan-fabric/rest/dashboard/switch/info?switchId=6980
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: switchId
      value: "6980"
      type: query
      description: switch db id
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
