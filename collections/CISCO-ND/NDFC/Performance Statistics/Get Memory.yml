info:
  name: Get Memory
  type: http
  seq: 38

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/pm/stat/memoryStatES?interval=Day&navId=-1&swId=-1
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: interval
      value: Day
      type: query
    - name: navId
      value: "-1"
      type: query
    - name: swId
      value: "-1"
      type: query
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
