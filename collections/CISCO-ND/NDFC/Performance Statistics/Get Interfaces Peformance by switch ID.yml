info:
  name: Get Interfaces Peformance by switch ID
  type: http
  seq: 17

http:
  method: GET
  url: https://{{server-address}}/appcenter/cisco/ndfc/api/v1/pm/stat/getInterfacesPMDataBySwitch
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  params:
    - name: switchDbID
      value: "-1"
      type: query
      disabled: true
    - name: interval
      value: Day
      type: query
      disabled: true
  body:
    type: json
    data: // /api/v1/schemas/{id}/templates/{templateName}/anps/{anpName}/epgs/{epgName}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
