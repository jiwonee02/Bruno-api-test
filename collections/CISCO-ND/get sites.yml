info:
  name: get sites
  type: http
  seq: 9

http:
  method: GET
  url: https://{{server-address}}/api/v1/sites
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
