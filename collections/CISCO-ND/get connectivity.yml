info:
  name: get connectivity
  type: http
  seq: 11

http:
  method: GET
  url: https://{{server-address}}/mso/api/v3/sites/fabric-connectivity-status
  headers:
    - name: cookie
      value: AuthCookie={{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
