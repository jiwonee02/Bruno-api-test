info:
  name: createPost
  type: http
  seq: 2

http:
  method: POST
  url: https:///jsonplaceholder.typicode.com/posts
  body:
    type: json
    data: |-
      {
        "title": "Hello HTTP",
        "body": "Learning Insomnia",
        "userId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
